# =============================================================================
# ENVIRONMENT VARIABLES REFERENCE
# =============================================================================
# Copy this file to .env and fill in your values
# For Vercel deployment, add these in the Vercel Dashboard → Settings → Environment Variables

# =============================================================================
# REQUIRED - Database Configuration
# =============================================================================
# PostgreSQL connection string
# For local development: Use your local PostgreSQL URL
# For Vercel deployment with Supabase: Use pooled connection (port 6543 with ?pgbouncer=true)
# Example: postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL=

# =============================================================================
# REQUIRED - Supabase Authentication (Backend)
# =============================================================================
# Your Supabase project URL (e.g., https://xxxxx.supabase.co)
SUPABASE_URL=

# Supabase anonymous/public key (safe to expose on frontend)
SUPABASE_ANON_KEY=

# Supabase service role key (NEVER expose on frontend - server-side only!)
# This is CRITICAL for authentication to work on Vercel
SUPABASE_SERVICE_ROLE_KEY=

# =============================================================================
# REQUIRED - Supabase Frontend Variables
# =============================================================================
# These must be prefixed with VITE_ to be accessible in the frontend
# Should match the backend SUPABASE_URL and SUPABASE_ANON_KEY
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# =============================================================================
# OPTIONAL - Payment Integration
# =============================================================================
# Paystack secret key (server-side only - for payment verification)
PAYSTACK_SECRET_KEY=

# Paystack public key (frontend - for payment initialization)
VITE_PAYSTACK_PUBLIC_KEY=

# =============================================================================
# OPTIONAL - Admin Configuration
# =============================================================================
# Legacy admin API key for programmatic product creation
# Only needed if using POST /api/admin/products endpoint with API key auth
ADMIN_API_KEY=

# =============================================================================
# OPTIONAL - Server Configuration
# =============================================================================
# Port for local development (default: 5000)
PORT=5000

# Environment mode (development or production)
NODE_ENV=development

# =============================================================================
# OPTIONAL - Replit Integration (Only if deploying on Replit)
# =============================================================================
# These are automatically set by Replit, only needed for Replit deployments
# REPL_ID=
# REPLIT_DOMAINS=
# ISSUER_URL=
# SESSION_SECRET=

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# For Vercel Deployment:
# 1. Go to Vercel Dashboard → Your Project → Settings → Environment Variables
# 2. Add ALL required variables (DATABASE_URL, SUPABASE_*, VITE_SUPABASE_*)
# 3. Add optional variables as needed (PAYSTACK_*, ADMIN_API_KEY)
# 4. IMPORTANT: Redeploy after adding/changing environment variables!
#
# For Local Development:
# 1. Copy this file to .env
# 2. Fill in all REQUIRED variables
# 3. Fill in OPTIONAL variables if you need those features
# 4. Never commit .env to version control!
#
# Database Connection Strings:
# - Local PostgreSQL: postgresql://user:password@localhost:5432/dbname
# - Neon: postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
# - Supabase (Pooled for Vercel): postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
# - Supabase (Direct): postgresql://postgres:[password]@db.[ref].supabase.co:5432/postgres
#
# Finding Supabase Credentials:
# 1. Go to your Supabase project dashboard
# 2. Settings → API → Copy Project URL and API Keys
# 3. Settings → Database → Copy Connection String (use Pooling for Vercel)
#
